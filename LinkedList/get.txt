public ListElement<T> get(int idx)
{
    if(isEmpty()|| idx >= size() || idx < 0) return null;
    int counter = 0;
    ListElement<T> tmp = getFirst();
    while(counter < idx) {
        tmp = tmp.next();
        counter++;
    }
    return tmp;
}